const assert = require('assert');
const { it } = require('mocha');

beforeEach(async ()=>{
    console.log("run")
});

describe('Inbox', ()=>{
    it('deploys a contract',()=>{
        assert.ok(true)
    });
});